name: Deployment locked pending AP-09.4

on:
  workflow_dispatch:

jobs:
  deployment-lock:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment intentionally disabled
        run: |
          echo "Automatic production deployment is intentionally disabled."
          echo "AP-09.4 must verify the exact source, target and rollback contract."
          echo "No files were deployed."
